# AcademicAgent Environment Variables
# IMPORTANT: Copy to .env and fill with real values. NEVER commit .env to git!

# ============================================================================
# Claude API Configuration
# ============================================================================
# Required: Your Anthropic API key
# Obtain from: https://console.anthropic.com/settings/keys
# Security: Use short-lived API keys, rotate regularly (30-90 days)
ANTHROPIC_API_KEY=[YOUR-KEY]

# Optional: Organization ID (for team accounts)
# ANTHROPIC_ORG_ID=

# ============================================================================
# DBIS Portal Configuration (University Database Access)
# ============================================================================
# Optional: Path to persistent session file (for authenticated DBIS access)
# If unset, you'll need to re-authenticate each run
# Security: Use ephemeral sessions when possible (delete after each run)
DBIS_SESSION_FILE=~/.local/share/academicagent/dbis_session.json

# Optional: University proxy/VPN credentials (if required by your institution)
# UNIVERSITY_PROXY_URL=
# UNIVERSITY_PROXY_USER=
# UNIVERSITY_PROXY_PASS=

# ============================================================================
# Chrome/Browser Configuration
# ============================================================================
# Optional: Chrome profile directory (for persistent cookies/logins)
# Default: Temporary profile (deleted after each run)
# Security: Use temporary profile unless you need persistent logins
# CHROME_PROFILE_DIR=~/.local/share/academicagent/chrome_profile

# Optional: Chrome remote debugging port
# Default: 9222
# CHROME_DEBUG_PORT=9222

# ============================================================================
# Run Configuration
# ============================================================================
# Optional: Custom runs directory (default: ./runs)
# RUNS_DIR=./runs

# Optional: Run ID format (default: timestamp)
# RUN_ID_PREFIX=research_

# ============================================================================
# Encryption (for sensitive research data)
# ============================================================================
# Optional: Path to encryption key file (for encrypting PDFs/outputs at rest)
# If unset, files are stored in plaintext
# Security: Enable FileVault (macOS) or LUKS (Linux) for disk encryption
# ENCRYPTION_KEY_FILE=~/.local/share/academicagent/encryption.key

# ============================================================================
# Cost/Budget Controls
# ============================================================================
# Optional: Maximum cost per run (USD)
# Default: 10.00
# MAX_COST_PER_RUN=10.00

# Optional: Maximum total cost per month (USD)
# Default: 100.00
# MAX_COST_PER_MONTH=100.00

# ============================================================================
# Logging
# ============================================================================
# Optional: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Default: INFO
# LOG_LEVEL=INFO

# Optional: Enable verbose logging (all API calls, network traffic)
# Default: false
# VERBOSE_LOGGING=false

# ============================================================================
# Security Notes
# ============================================================================
# 1. NEVER commit .env to version control (already in .gitignore)
# 2. Use OS keychain for storing credentials (macOS Keychain, Linux Secret Service)
# 3. Rotate API keys every 30-90 days
# 4. Use separate API keys for dev/prod environments
# 5. Enable disk encryption (FileVault/LUKS) for sensitive research data
# 6. Delete temporary session files after each run:
#    rm -f ~/.local/share/academicagent/dbis_session.json
# 7. Use short-lived tokens when available (e.g., temporary API keys)
# 8. Principle of least privilege: Use read-only API keys when possible

# ============================================================================
# Troubleshooting
# ============================================================================
# If you see "ANTHROPIC_API_KEY not found" errors:
#   1. Copy this file to .env: cp .env.example .env
#   2. Edit .env and add your real API key
#   3. Verify: source .env && echo $ANTHROPIC_API_KEY
#
# If you see "Permission denied" errors:
#   1. Check file permissions: ls -la .env
#   2. Should be: -rw------- (600) - only owner can read/write
#   3. Fix with: chmod 600 .env
